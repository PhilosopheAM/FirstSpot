### 从「大模型能读什么」先说清楚

对大模型来说，**最靠谱的是结构化文字 + 必要的截图**，而不是「把 Figma/即刻的源文件直接丢给它」。所以你要做的是把「你脑子里的设计意图」翻译成**清晰的文字说明 + 少量关键画面**。

---

### 建议的输出方式（适用于任何大模型）

你可以按下面这个结构，把你的 UX 原型「翻译」出来：

- **1. 项目背景 & 目标用户**  
  - 这是一个什么产品？（例：面向新手投资者的记账+教育 App）  
  - 目标用户是谁？（新手/中阶，风险偏好，使用场景等）  
  - 这个版本的**核心目标**是什么？（拉新、首单、留存、教育完课率……）

- **2. 核心使用场景 / 用户路径**  
  尽量用「故事」描述，而不是只列页面名：  
  - 场景 1：用户第一次打开应用 → 完成哪些步骤？  
  - 场景 2：用户记一笔投资 → 需要看/填哪些信息？  
  - 场景 3：用户复盘自己的投资 → 看什么数据、做什么动作？

- **3. 页面列表（和即刻设计里的画板一一对应）**  
  比如：

  - 页面 A：`Onboarding_01_Welcome`  
    - 主要模块：欢迎文案、产品承诺、开始按钮  
    - 设计意图：降低陌生感、突出安全和专业性  
  - 页面 B：`Home_01_Dashboard`  
    - 主要模块：资产总览卡片、今日任务、最近操作入口  
    - 设计意图：一眼看清当前状况 + 引导完成关键动作

- **4. 对每个关键页面，描述 3 件事**

  对于每一个关键画板（Frame），给大模型文字描述：

  - **信息优先级**：  
    - 什么是用户「第一眼必须看到」的？  
    - 什么是辅助信息？  
  - **主要交互**：  
    - 用户可以点哪里？  
    - 每个操作期望触发什么结果（跳转、弹窗、状态变化）？  
  - **设计考量**：  
    - 为什么这样排版？  
    - 是否考虑了某些 UX 原则（如减少输入、分步引导、预填默认值）？

- **5. 补充：状态 & 边界情况**

  - 正常态、加载态、空数据态、错误态、禁用态，你有没有在原型里设计？  
  - 如果有，就说明：什么时候会出现这些页面/组件的不同状态。

---

### 如何结合「截图」一起给大模型？

- 在即刻设计中，**导出关键页面为 PNG/JPG**：  
  - 比如：`home_dashboard.png`, `task_detail.png` 等  
- 在给大模型时：  
  - 一边发图（路径或上传），  
  - 一边配上「文字版结构说明」，类似：

    > 这是首页截图 `home_dashboard.png`：  
    > - 顶部：资产总览卡片（显示总市值、今日盈亏）  
    > - 中部：三个快捷入口（记一笔、查看任务、学习课程）  
    > - 底部：Tab 导航（首页 / 记录 / 课程 / 我的）  
    > 设计意图：希望用户打开 App 的第一件事，是先确认自己整体资产状况，其次完成“记一笔”或“学习课程”的关键任务。

这样做的好处是：**即使模型暂时看不清细节，也能从文字里准确理解你的设计决策**。

---

### 一个你可以直接复用的「提示词模板」

以后你可以直接按这个格式把你的 UX 原型交给大模型（下面是示意，你可以替换成自己的内容）：

```text
我在即刻设计里做了一个移动端 App 的 UX 原型。请你以「资深移动端 UX 设计师 + 游戏化产品经理」的视角，先理解我的设计意图，再给出优化建议。

【产品背景】
- 产品：FirstSpot，面向新手投资者的投资账本 + 投资者教育 App
- 目标用户：25–35 岁，有一定储蓄，刚开始接触基金/股票，风险偏好保守偏中性
- 本次版本目标：让用户形成「每天打开 App 看一眼并记一笔」的习惯，并在 7 天内完成一套入门教育任务

【核心使用场景】
1. 首次打开 → 注册/登录 → 完成简单风险测评 → 进入首页
2. 每日打开 → 查看资产总览 → 完成今日任务 → 记一笔新的投资记录
3. 每周一次 → 打开「复盘」页 → 查看本周盈亏和完成的学习任务

【页面列表与说明】
- 页面 A：Onboarding_01_Welcome（对应截图：onboarding_01.png）
  - 模块：欢迎标题、副标题、开始按钮
  - 意图：降低心理门槛，强调“陪你长期学习投资，而不是赌短线”

- 页面 B：Home_01_Dashboard（对应截图：home_dashboard.png）
  - 模块：资产卡片、今日任务卡片、最近记录列表、底部导航
  - 信息优先级：资产卡片 > 今日任务 > 最近记录
  - 主要交互：点击资产卡片 → 进入资产详情页；点击今日任务 → 进入任务详情；点击记录行 → 进入记录详情

【请你帮我做的事】
1. 先复述一遍你理解到的产品目标和首页信息架构，看是否和我一致；
2. 以移动端 UX 视角，指出首页布局是否合理，有没有信息过载或缺失；
3. 以游戏化产品视角，看看「今日任务 + 资产卡片」这样的设计，是否有更好的激励方式。
```

在 Cursor 里，你可以：

- 把这段文字直接发给我；  
- 如果你把导出的图片放到项目里，也可以告诉我路径（比如 `design/home_dashboard.png`），我就可以结合截图和文字一起理解。

---

### 总结一句话

**不要指望「直接读设计文件」就能懂你的脑子，而是要主动用结构化文字，告诉大模型：为什么这样设计、希望用户怎么用、你最在意什么。**  
只要你按上面的结构来写，我就能非常接近真实地还原你的设计意图，并且用你约定的那几个专业视角来和你一起打磨。