---
description: 任务分发 - 用户布置任务时触发；若以 guide: 或 提问 开头则不适用
alwaysApply: true
---

# 任务分发 (Task Dispatch)

## 【执行前必须检查】排除场景（优先于下方所有触发条件）

若用户消息以 **`guide:`** 或 **`提问`** 开头（含前后有空格），则**本规则不适用**：不读取、不应用 skill-dispatcher，由 flutter-guide-trigger 规则处理。

---

当用户布置**实现类任务**且不满足上述排除条件时，优先应用 skill-dispatcher 进行分析与指派。

## 触发场景

用户请求包含以下任一特征时，**先**读取并应用 `.cursor/skills/skill-dispatcher/SKILL.md`：

- 修改类：修改、改、调整、更新
- 实现类：实现、添加、创建、做
- 请求类：帮我、请、需要
- 具体动作：改颜色、加动画、设计、测试、创建页面

## 执行顺序

1. 应用 skill-dispatcher（分析任务 → 匹配技能 → 列出方案）
2. 按 skill-dispatcher 的匹配结果，依次读取并应用对应 Skills
3. 输出遵循 firstspot-coding-standards 规范
